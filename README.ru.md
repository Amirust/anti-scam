# Mr Beast / Melstroy anti-scam Discord bot

🇬🇧 [English version](README.md)

Discord-бот, который ловит известные скам-картинки (фейковые розыгрыши,
казино-спам и подобное), рассылаемые со взломанных аккаунтов, банит
отправителя и отчитывается в админ-канал.

Схема скамеров всегда одна: угнали аккаунт — и один и тот же скриншот летит в
десятки каналов. Сравнение по байтам не работает — каждая копия пережата,
отресайжена или слегка подредактирована, поэтому бот использует перцептивные
хеши, откалиброванные так, чтобы переживать именно эти трансформации.

## Что делает бот

Когда приходит сообщение с картинкой — прямым вложением, вложением
пересланного (forward) сообщения или ссылкой, которую Discord разворачивает в
превью-эмбед, — бот:

1. Скачивает картинку (одинаковые картинки от одного автора, обрабатываемые
   одновременно дедуплицируются на лету). Превью ссылок качаются только через
   медиа-прокси Discord — по сторонним хостам бот не ходит.
2. Прогоняет её через пайплайн детекта (ниже) по датасету известных
   скам-картинок.
3. Исполняет вердикт:
   - **Ban** — отправляет пользователю объяснение в личку (best-effort), банит
     его с именем совпавшей записи датасета в причине для аудит-лога, удаляет
     его недавние сообщения и постит отчёт-эмбед с картинкой в админ-канал
     гильдии. Если у бота не хватает прав на бан — вместо этого постит отчёт
     «не смог забанить».
   - **Review** — совпадение слабее порога бана; постит отчёт с картинкой в
     админ-канал с кнопкой **Ban user** (для модераторов с правом Ban Members;
     заодно удаляет сообщения пользователя начиная с помеченного поста, максимум
     за 7 дней — лимит Discord). Состояние кнопок живёт в самом сообщении,
     поэтому они переживают рестарты бота.
   - **Clean** — ничего не происходит.

На каждом отчёте есть также кнопка **Add to dataset** — один из способов
пополнять датасет прямо из Discord, см.
[Пополнение датасета из Discord](#пополнение-датасета-из-discord).

## Как работает детект

Сырые хеши (sha256) здесь бесполезны: скамеры пережимают каждую копию, байты
всегда разные. Поэтому:

**0. Нормализация.** Каждая картинка ресайзится до 256×256 (Lanczos) и
переводится в оттенки серого.

**1. Перцептивный хеш целой картинки.** pHash 8×8 (DCT median) всей картинки
сравнивается с каждой записью датасета. Два дешёвых трюка обхода
компенсируются дополнительными пробными видами входящей картинки: наклон
скриншота (хеши на поворотах в пределах ±12° с шагом 3°) и добавленные поля —
белые рамки, тёмные подложки, «скриншот скриншота» (хеш картинки с
обрезанными почти-однотонными краями). Считается минимальная дистанция по
всем видам. Дистанция Хэмминга ≤ 12 из 64 бит — жёсткое совпадение.
Откалибровано на эталонном наборе: пережатые копии одной картинки дают 0–6,
наклонённые / с полями / перекадрированные копии остаются в пороге, чужие
пары — 18+.

**2. Тайловая проверка.** Если целая картинка не совпала, бот ищет частично
перерисованные варианты. Картинка режется на сетку 4×4 из тайлов 64×64,
каждый хешируется отдельно. Тайловый pHash разваливается уже от сдвига в 2 px,
а пережатые копии гуляют на ~5 px, поэтому входящая картинка сначала
примеряется на всех сдвигах в пределах ±6 px с шагом 2 px (49 вариантов), и
берётся сетка с лучшим выравниванием.

**3. Информативность тайла.** Тайл учитывается, только если дисперсия его
яркости больше 150 — плоские фоны дают меньше ~120, реальный контент — от
~200. Это не даёт пустым полям голосовать.

**4. Подсчёт.** Для каждой записи датасета тайл считается *информативным*,
если он информативен в обеих картинках, и *совпавшим*, если дистанция Хэмминга
≤ 13. Если сравнили больше 6 информативных тайлов:

- совпало ≥ 75% информативных → **Ban**
- совпало ≥ 60% → **Review**
- иначе → **Clean**

Все пороги настраиваются через [`config.toml`](#конфигурация).

## Датасет запрещённых картинок

На старте бот читает `banned.json` (путь меняется через env-переменную
`BANNED_CONFIG`). Нет файла — бот пишет предупреждение в лог и стартует с
пустым датасетом. В файле только хеши — исходные картинки из него
восстановить нельзя.

Взять датасет можно двумя способами:

- **Скачать официальный:** `./get-config.sh` качает JSON из releases
  репозитория и сверяет sha256 с `banned.json.sha256`. Если локальный
  `banned.json` уже существует, скрипт спросит перед перезаписью (локальные
  добавления иначе пропадут). Перед запуском скрипт стоит прочитать.
- **Собрать свой:** сложите скриншоты скама в папку и запустите
  `anti-scam export <папка> [banned.json]`. Каждая картинка прогоняется через
  хеш-пайплайн; команда пишет конфиг и печатает его sha256 для файла
  контрольной суммы. На один тип скама обычно хватает одного-двух эталонов.

Датасет привязан к версии хеш-пайплайна (`pipeline_version`). Если алгоритм
хеширования поменялся, бот не примет старый конфиг — скачайте свежий или
перегенерируйте через `anti-scam export`.

### Пополнение датасета из Discord

Новые скам-шаблоны появляются быстрее, чем кто-то перезапускает `export`,
поэтому датасет можно пополнять, не заходя на сервер:

- **Из отчёта** — на каждом бан/ревью-отчёте в админ-канале есть кнопка
  **Add to dataset**. Она открывает модалку с именем записи (опционально —
  оставьте пустым для автогенерации).
- **Из любого сообщения** — правый клик по сообщению → **Приложения** →
  **Add image to dataset**. Модалка дополнительно спросит, какую картинку
  брать, если их в сообщении несколько (по умолчанию — первая).
- **Из лички бота** — контекстная команда работает и в личных сообщениях.
  Увидели свежий скам где-то ещё? Перешлите сообщение (или отправьте
  картинку) боту в личку, правый клик — готово. Пересланные сообщения
  полностью поддерживаются.

Оба пути доступны только владельцу бота: пункт контекстного меню видят
администраторы, но выполнить команду может только владелец. Новые записи
дописываются в локальный `banned.json` и подхватываются на лету — рестарт не
нужен. Картинки, уже жёстко совпадающие с существующей записью, отклоняются
как дубликаты, а имена записей должны быть уникальны.

## DINOv2 shadow-режим (экспериментальный)

Перцептивные хэши ловят пережатые копии известной картинки, но не тянут разные
кропы, масштабы и перерисованные варианты одного шаблона. Эмбеддинг-стадия
закрывает этот разрыв: каждая картинка кодируется DINOv2-S в 384-мерный вектор
и сравнивается по косинусной близости с эмбеддингами известного скама.

Работает в **shadow-режиме**: никогда не банит, ничего не удаляет и не влияет
на вердикт хэш-пайплайна. Его задача — сначала собрать данные для калибровки:

- Каждая просканированная картинка получает строку в sqlite
  (`dino_observations`): ближайшая запись датасета, косинусная близость и
  вердикт хэш-пайплайна (`ban`/`review`/`clean`). Строки чистого трафика
  формируют распределение негативов, которого датасету не хватает; строки
  подтверждённых хэшами банов — бесплатные позитивы.
- Когда хэши сказали clean и review-гейт сработал, в админ-канал приходит
  карточка разметки: **✅ Scam (hash missed it)** / **❌ Not a scam** /
  **⚠️ Legit but similar**. Для разметки нужно право Ban Members; побеждает
  первая метка, она сохраняется вместе с наблюдением.
- Метки кормят датасет на месте: подтверждённый скам становится новым
  **скам-референсом**, «legit but similar» — **негативным референсом**
  (известным легальным двойником). Review-гейт сравнительный: карточка
  отправляется, только если лучшая скам-близость превышает лучшую
  негативную минимум на `dino.negative_margin` — одна размеченная ложная
  тревога подавляет весь свой класс двойников, не трогая полноту по скаму.
  Метка «not a scam» только калибрует порог.
- Пиксели сохраняются для каждой метки и референса в `dino.captures_dir`
  (`<группа>/<имя>.<ext>`) — ре-экспорту после бампа пайплайна и прогонам
  eval нужны картинки, а не скаляры. Чистый трафик на диск никогда не
  пишется.
- На карточке есть и обычная кнопка **Add to dataset** (только владелец),
  чтобы сразу закрыть дыру на стороне хэшей.

Ручное управление датасетом из Discord (правый клик по сообщению → Apps):

- **DINO: add as scam** / **DINO: add as negative** — добавить картинку в
  эмбеддинг-датасет напрямую (только владелец, видно администраторам).
  Почти идентичные эмбеддинги и занятые имена отклоняются.
- **DINO: check image** — эфемерная диагностика близости для картинок
  сообщения: лучший матч, ближайший негатив и что сделал бы review-гейт.
  Нужно право Ban Members, ничего не изменяет.

Настройка:

```sh
# 1. энкодер (ONNX-экспорт Xenova/dinov2-small, ~85 МБ, fp32)
curl -L -o dinov2s.onnx \
  https://huggingface.co/Xenova/dinov2-small/resolve/main/onnx/model.onnx

# 2. референсные эмбеддинги из папки со скам-картинками (рекурсивно);
#    --negatives сеет известные легальные двойники, например из captures
anti-scam dino-export ./images [dino.json] [--negatives ./dino_captures/hard_negative]

# 3. включить в config.toml
#    [dino]
#    enabled = true
```

Калибровка без живого трафика: `anti-scam dino-classify <папка> [dino.json]`
оценивает каждую картинку папки против датасета и печатает TSV (лучшее и
второе совпадение) со сводкой в stderr. Варианты известного шаблона обычно
дают 0.7–1.0, та же картинка ~1.0; принципиально другие макеты набирают
меньше и должны становиться отдельными записями датасета — так же, как в
хэш-пайплайне.

Заметки: добавления из Discord (метки и контекстные команды) обновляют файл
датасета и работающего бота на лету; рестарт нужен только когда файл
пересобран снаружи через `dino-export` (не запускайте его, пока бот пишет в
тот же файл). Датасет привязан к своей версии пайплайна — всё, что меняет
вычисление эмбеддингов, требует ре-экспорта. Отсутствующий файл датасета
стартует shadow-режим пустым (наполняйте из Discord); битая модель или
датасет валят старт. Хэш-датасет (`banned.json`) и эмбеддинг-датасет
раздельны: кнопка **Add to dataset** кормит первый, DINO-команды — второй.

## HTTP API (экспериментальный)

Внешние сервисы могут присылать картинки на проверку по HTTP. Отправитель
всегда сразу получает `200 {"status":"accepted"}` — классификация идёт в
фоне, результат отправителю не возвращается. Когда DINO-стадия флагает
картинку (основной сигнал) или совпадают хэши, карточка-репорт с обоими
сигналами и самой картинкой падает в настроенный канал
(`api.report_guild_id` / `api.report_channel_id`). Никого не банит и ничего
не удаляет. На карточках есть обычные кнопки DINO-разметки, так что
API-заливки кормят калибровку и датасет наравне с остальными.

```sh
# на сервере: секрет в окружении, эндпоинт в config.toml ([api])
export API_JWT_SECRET=<случайная строка, 32+ символов>

# выпустить токен клиенту (HS256 JWT; тот же секрет, что у сервера)
anti-scam issue-token my-client 365

# на клиенте: сырые байты картинки, Bearer-авторизация
curl -X POST http://127.0.0.1:8080/v1/check \
  -H "Authorization: Bearer <token>" \
  --data-binary @image.jpg
```

Ответы: `200` принято, `401` плохой/просроченный токен, `400` пустое тело,
`413` больше лимита в 20 МБ, `429` слишком много заливок в обработке (каждая
стоит инференс — повторите позже). Эндпоинт отдаёт чистый HTTP: держите
`api.bind` на loopback, а TLS и rate limiting вешайте на reverse proxy,
когда выставляете наружу.

## Установка

Требования: Rust (edition 2024).

```sh
export DISCORD_TOKEN=токен-бота
cargo run --release
```

База sqlite (`data.db`) создаётся и мигрируется автоматически при первом
запуске.

Боту нужен gateway-интент **Message Content** (включается в Discord developer
portal) и права **Ban Members**, **Send Messages** и **Embed Links** в
гильдии.

Дальше в каждой гильдии администратор задаёт канал для отчётов:

```
/settings set_notification_channel #канал
```

Настройки гильдий хранятся в sqlite и отдаются из LRU-кэша в памяти, поэтому
обычная обработка сообщений базу не трогает.

### Docker

```sh
cp .env.example .env          # впишите туда DISCORD_TOKEN
mkdir -p config data
cp config.toml config/        # опционально, без него работают дефолты
cp banned.json config/        # или скачайте: ./get-config.sh config/banned.json
docker compose up -d --build
```

`banned.json` и `config.toml` лежат на хосте в `./config` и монтируются в
контейнер директорией — записи, добавленные кнопкой **Add to dataset**,
попадают в файл на хосте. База sqlite сохраняется в `./data`.

Для shadow-режима положите `dinov2s.onnx` и `dino.json` тоже в `./config` и
укажите в `config.toml` пути `dino.model_path` / `dino.dataset_path` на
`/config/...`.

## Конфигурация

Опциональна. Бот читает `config.toml` из рабочей директории (путь меняется
через env-переменную `CONFIG_PATH`); отсутствующие ключи или отсутствующий
файл откатываются на дефолты. Файл читается один раз на старте.

| Ключ | Дефолт | Значение |
|------|--------|----------|
| `detection.whole_match_threshold` | 12 | Макс. дистанция Хэмминга (из 64 бит) для совпадения целой картинки |
| `detection.tile_match_threshold` | 13 | Макс. дистанция Хэмминга для совпадения тайла |
| `detection.min_informative_tiles` | 6 | Минимум информативных тайлов для доверия тайловому вердикту |
| `detection.hard_match_percent` | 75 | Процент совпавших тайлов для автобана |
| `detection.review_percent` | 60 | Процент совпавших тайлов для отправки на ручную проверку |
| `cache.guild_settings_capacity` | 100 | Сколько гильдий держится в LRU-кэше настроек |
| `dino.enabled` | `false` | Эмбеддинг shadow-режим ([детали](#dinov2-shadow-режим-экспериментальный)) |
| `dino.model_path` | `./dinov2s.onnx` | ONNX-энкодер DINOv2-S |
| `dino.dataset_path` | `./dino.json` | Эмбеддинг-датасет из `dino-export` |
| `dino.review_threshold` | 0.6 | Мин. косинусная близость для карточки разметки |
| `dino.negative_margin` | 0.05 | Скам-близость должна превышать лучший негатив на столько |
| `dino.intra_threads` | 2 | Потоки ONNX Runtime на инференс |
| `dino.captures_dir` | `./dino_captures` | Куда сохраняются картинки меток и референсов |
| `api.enabled` | `false` | HTTP-эндпоинт проверки ([детали](#http-api-экспериментальный)) |
| `api.bind` | `127.0.0.1:8080` | Адрес API |
| `api.report_guild_id` | — | Гильдия канала репортов (обязательно при включении) |
| `api.report_channel_id` | — | Канал репортов о зафлаганных заливках |

Это пороги времени сравнения — их подкрутка никогда не инвалидирует
существующий `banned.json`.

### Переменные окружения

| Переменная | Дефолт | Значение |
|------------|--------|----------|
| `DISCORD_TOKEN` | — (обязательна) | Токен бота |
| `BANNED_CONFIG` | `./banned.json` | Путь к датасету запрещённых картинок |
| `CONFIG_PATH` | `./config.toml` | Путь к рантайм-конфигу |
| `API_JWT_SECRET` | — (обязательна при `api.enabled`) | HS256-секрет для API-токенов, 32+ символов |
